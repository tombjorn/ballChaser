function setup() {
	createCanvas(windowWidth, windowHeight);

createCanvas(windowWidth, windowHeight);
}

function draw() {
background(0);
fill(255);
ellipse(width/2, height/2, 100);
rect(0,0,100,50);
}
